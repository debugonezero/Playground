# REPO_SPEC: AXIOM_PLAYGROUND
HOST_TYPE: Stateless WebGPU Receiver
CORE_ENGINE: new Function('p', 'state', p.js)
GLOBAL_NAMESPACE:
  - state.device: Persistent GPUDevice
  - state.ctx: GPUCanvasContext
  - state.size: {w, h}
  - state.currentId: Payload Identity
INJECTION_RULES:
  - p.html is injected into #canvas-root via innerHTML
  - p.js executes via Function constructor
  - rAF loops must verify state.currentId == p.id
